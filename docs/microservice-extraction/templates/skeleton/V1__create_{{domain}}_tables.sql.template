-- Copy ONLY the tables owned by {{domain}} verbatim from the monolith's V1__create_tables.sql.
-- Example below is the Favorite domain (article_favorites). Replace for other domains per
-- docs/microservice-extraction/templates/03-extracted-service-template.md §9.
create table article_favorites (
  article_id varchar(255) not null,
  user_id varchar(255) not null,
  primary key(article_id, user_id)
);
