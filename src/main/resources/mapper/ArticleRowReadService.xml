<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.infrastructure.mybatis.readservice.ArticleRowReadService">
    <sql id="selectArticleRow">
        select
        A.id articleId,
        A.slug articleSlug,
        A.title articleTitle,
        A.description articleDescription,
        A.body articleBody,
        A.user_id articleUserId,
        A.created_at articleCreatedAt,
        A.updated_at articleUpdatedAt,
        T.name tagName
        from
        articles A
        left join article_tags AT on A.id = AT.article_id
        left join tags T on T.id = AT.tag_id
    </sql>
    <sql id="fromArticleIds">
        from
        articles A
        left join article_tags AT on A.id = AT.article_id
        left join tags T on T.id = AT.tag_id
    </sql>
    <sql id="idFilters">
        <if test="tag != null">
            T.name = #{tag}
        </if>
        <if test="authorId != null">
            AND A.user_id = #{authorId}
        </if>
        <if test="articleIds != null">
            AND A.id in
            <foreach collection="articleIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
    </sql>
    <sql id="cursorFilters">
        <if test='page.cursor != null and page.direction.name() == "NEXT"'>
            AND A.created_at &lt; #{page.cursor}
        </if>
        <if test='page.cursor != null and page.direction.name() == "PREV"'>
            AND A.created_at > #{page.cursor}
        </if>
    </sql>
    <sql id="cursorOrder">
        <if test='page.direction.name() == "NEXT"'>
            order by A.created_at desc
        </if>
        <if test='page.direction.name() == "PREV"'>
            order by A.created_at asc
        </if>
    </sql>

    <select id="findById" resultMap="articleRow">
        <include refid="selectArticleRow"/>
        where A.id = #{id}
    </select>
    <select id="findBySlug" resultMap="articleRow">
        <include refid="selectArticleRow"/>
        where A.slug = #{slug}
    </select>
    <select id="findArticles" resultMap="articleRow">
        <include refid="selectArticleRow"/>
        where A.id in
        <foreach collection="articleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        order by A.created_at desc
    </select>
    <select id="queryArticleIds" resultMap="articleId">
        select DISTINCT(A.id) articleId, A.created_at
        <include refid="fromArticleIds"/>
        <where>
            <include refid="idFilters"/>
        </where>
        order by A.created_at desc
        limit #{page.offset}, #{page.limit}
    </select>
    <select id="countArticleIds" resultType="java.lang.Integer">
        select count(DISTINCT A.id)
        <include refid="fromArticleIds"/>
        <where>
            <include refid="idFilters"/>
        </where>
    </select>
    <select id="queryArticleIdsWithCursor" resultType="java.lang.String">
        select DISTINCT(A.id) articleId, A.created_at
        <include refid="fromArticleIds"/>
        <where>
            <include refid="idFilters"/>
            <include refid="cursorFilters"/>
        </where>
        <include refid="cursorOrder"/>
        limit #{page.queryLimit}
    </select>
    <select id="findArticlesOfAuthors" resultMap="articleRow">
        <include refid="selectArticleRow"/>
        where A.user_id in
        <foreach collection="authorIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        limit #{page.offset}, #{page.limit}
    </select>
    <select id="countArticlesOfAuthors" resultType="java.lang.Integer">
        select count(1) from articles A where A.user_id in
        <foreach collection="authorIds" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
    <select id="findArticlesOfAuthorsWithCursor" resultMap="articleRow">
        <include refid="selectArticleRow"/>
        <where>
            A.user_id in
            <foreach collection="authorIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
            <include refid="cursorFilters"/>
        </where>
        <include refid="cursorOrder"/>
        limit #{page.queryLimit}
    </select>

    <resultMap id="articleRow" type="io.spring.application.data.ArticleRow">
        <id column="articleId" property="id"/>
        <result column="articleSlug" property="slug"/>
        <result column="articleTitle" property="title"/>
        <result column="articleDescription" property="description"/>
        <result column="articleBody" property="body"/>
        <result column="articleUserId" property="userId"/>
        <result column="articleCreatedAt" property="createdAt"/>
        <result column="articleUpdatedAt" property="updatedAt"/>
        <collection property="tagList" javaType="list" ofType="string">
            <result column="tagName"/>
        </collection>
    </resultMap>
    <resultMap id="articleId" type="string">
        <id javaType="string" column="articleId"/>
    </resultMap>
</mapper>
