<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.infrastructure.mybatis.readservice.TagReadService">
    <select id="all" resultType="string">
        select name from tags
    </select>
    <select id="articleTagRows" resultType="io.spring.infrastructure.mybatis.readservice.ArticleTagRow">
        select AT.article_id as articleId, T.name as name
        from article_tags AT
        join tags T on T.id = AT.tag_id
        where AT.article_id in
        <foreach collection="articleIds" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        order by AT.rowid
    </select>
    <select id="articleIdsByTag" resultType="string">
        select distinct AT.article_id
        from article_tags AT
        join tags T on T.id = AT.tag_id
        where T.name = #{tagName}
    </select>
</mapper>
